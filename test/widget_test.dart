// test/widget_test.dart
import 'package:midi_pedal_manager/main.dart'; // Verifique se o caminho está certo

// Se o erro persistir, você pode simplesmente comentar o conteúdo do main() deste arquivo
// ou garantir que o nome da classe aqui seja o mesmo que está no seu main.dart (ex: MyApp)